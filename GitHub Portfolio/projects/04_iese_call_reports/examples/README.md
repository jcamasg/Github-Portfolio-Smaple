# Examples

No rendered output is committed because the authorised WRDS inputs are not
included. After rendering locally, write results to this project's `outputs/`
directory and review the document for compliance with source-data terms before
publishing.
